# JSON → VLESS / Sing-box / Clash (PWA)

A clean, single-page web panel that converts a JSON config into:
- **VLESS URI**
- **Sing-box config**
- **Clash config**

Designed to be **minimal**, **fast**, and **easy to use**.  
Deployed via **GitHub Pages** and installable as a **PWA** in Chrome.

---

## ✨ Features

- **Paste JSON → Convert instantly**
- Outputs:
  - **VLESS link** (ready to import)
  - **Sing-box JSON** config
  - **Clash YAML** config
- **One-click copy** buttons (full-width and mobile-friendly)
- **QR code** generation for the VLESS URI (handles long links)
- **Modern UI**: compact, clean layout, no clutter
- **PWA ready**:
  - Installable on **Android/Windows/macOS** via Chrome
  - Works offline for the UI (QR image requires internet if using the remote generator)

---

## 🚀 Live Demo (GitHub Pages)

After enabling GitHub Pages, your app will be available at:

`https://<YOUR_USERNAME>.github.io/<REPO_NAME>/`

---

## 📦 Project Structure

Place all files in the **repository root**:

